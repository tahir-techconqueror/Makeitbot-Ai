import { AgentResponseCarousel } from '@/components/chat/agent-response-carousel';

describe('AgentResponseCarousel', () => {
    it('is defined and can be imported', () => {
        expect(AgentResponseCarousel).toBeDefined();
    });
});
